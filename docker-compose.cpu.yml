services:
  ai-worker:
    gpus: !reset []
    environment:
      AI_DEVICE: cpu
  motionclip-worker:
    gpus: !reset []
    environment:
      MOTIONCLIP_DEVICE: cpu
  kinecal-risk-worker:
    gpus: !reset []
    environment:
      KINECAL_DEVICE: cpu
  ollama:
    gpus: !reset []
